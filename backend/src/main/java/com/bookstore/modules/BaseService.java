package com.bookstore.modules;

/**
 * Common base interface for all service layer interfaces in the modules package.
 * Every module-level service interface (CartService, WishlistService, etc.)
 * must extend this interface to enforce a consistent service contract.
 */
public interface BaseService {
}
